The Simple flask applications template
======================================

It's yet another flask boilerplate for fast start.

NOTE: Don't forget setup your app name in `manage.py` and in `accounts/models.py`